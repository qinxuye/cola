name: cola
description: a distributed crawler framework
opener:
  user-agent: Mozilla/5.0 (compatible; MSIE 9.0; Windows NT 6.1; Trident/5.0)
master:
  port: 11103
worker:
  port: 11203
client:
  port: 11303
job:
  db: cola
  mode: url # also can be `bundle`
  size: -1   # the destination (including bundle or url) size, -1 means not limited
  speed: 
    max: -1  # -1 means no restrictions, if greater than 0, means webpages opened per minute.
    adaptive: yes
  instances: 1
  priorities: 3 # priorities queue's count in mq